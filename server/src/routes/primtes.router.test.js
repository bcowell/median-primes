import router from './index.js';
import request from 'supertest';

describe('Integration testing route', () => {

    test('Success response on GET method', async () => {
        expect(true).toBe(true);
        //const response = await request(
    });
});
